<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.temi_app" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >

        <activity android:name=".OrderFood_Page" />
        <activity android:name=".More_OrderFood_page"
            android:parentActivityName=".OrderFood_Page"/>

        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Arm -->
        <activity android:name=".P9"></activity>
        <activity android:name=".P7" />
        <activity android:name=".P7_No_Receipt" />

    </application>

</manifest>